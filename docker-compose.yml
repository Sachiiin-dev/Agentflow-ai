services:
  mongodb:
    image: mongo:7
    container_name: agentflow-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: agentflow_ai
    volumes:
      - agentflow-mongodb-data:/data/db

volumes:
  agentflow-mongodb-data:
